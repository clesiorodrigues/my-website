import React from 'react';
import { CenterSection } from '../../styles';

const Repos = () => {
    return(
        <CenterSection BGC="blue">

        </CenterSection>
    );
}

export default Repos;